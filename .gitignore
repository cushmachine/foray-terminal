node_modules
dist
dist.next/
.env
.playwright/
.env.*
*.log
.DS_Store
# Per-developer Claude Code settings, if a contributor creates them.
.claude/settings.local.json

# Per-machine pm2 env overrides (see ecosystem.config.cjs).
ecosystem.local.cjs
